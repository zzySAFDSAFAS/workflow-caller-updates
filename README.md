# Workflow Caller Updates

This repository hosts public update metadata for Workflow Caller.

No source code, private configuration, API keys, or tokens are stored here.

## Release Version Rule

Each packaged release increments the app version by `+0.0.1`.
For example, after `2.2.4`, the next packaged version is `2.2.5`.
