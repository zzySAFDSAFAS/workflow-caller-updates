# Workflow Caller 2.2.5

发布时间：2026-05-16

本次发布将应用版本号从 `2.2.4` 顺延到 `2.2.5`。

## 更新内容

- 按打包规则将软件版本号递增 `+0.0.1`。
- 在 GitHub 更新仓库文档中记录版本递增规则：每次打包版本号按 `+0.0.1` 顺延。
- 更新 `latest.json`，让旧版本客户端可以检查到 `2.2.5`。
